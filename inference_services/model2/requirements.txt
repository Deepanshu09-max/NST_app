fastapi
uvicorn[standard]
python-multipart
numpy
imageio
Pillow
tensorflow # Use CPU version for lighter image.
scipy
# Ensure you install CPU-only versions if not using GPU in Docker for lighter images
# Example: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu